# SPDX-FileCopyrightText: Copyright (c) 2025-2026, NVIDIA CORPORATION & AFFILIATES. All rights reserved.
# SPDX-License-Identifier: Apache-2.0

# Runs the helm-sync agent (.github/helm-sync/helm_sync_agent.py) on every
# push to a pull-request/<N> mirror branch where the **cumulative** PR
# diff (PR base ↔ HEAD) touches anything under deploy/ or this harness.
# The agent diffs the FULL PR (base...mirror, accumulated commits — never
# just the latest push) and decides whether the docker-side changes are
# reflected in the helm chart. Drift → bot PR + comment on the source PR.
# See .github/helm-sync/AGENTS.md for the agent's system prompt.

name: Helm Sync

on:
  # No top-level `paths:` filter and no `on: create:`. Mirroring
  # skills-eval's lesson: GitHub's trigger-level `paths:` evaluates the
  # diff of the *pushed commits*, not the cumulative PR diff against
  # base. That silently skips the workflow when CPR-bot pushes a merge
  # commit from base that doesn't itself touch deploy/** (the cumulative
  # PR diff might still touch deploy/**). The path gate is now done
  # inside the job by `dorny/paths-filter` against `PR_BASE...HEAD`.
  push:
    branches:
      - "pull-request/[0-9]+"

# Workflow-scoped GITHUB_TOKEN permissions. The bot uses
# `${{ github.token }}` directly (no PAT, no rotation, no personal-
# account binding) to push helm-sync-bot/pr-<N> and comment on the
# source PR. `actions/checkout@v4` injects this token via
# http.extraheader, so `git push` and `gh` calls inherit it
# automatically — IF gh CLI doesn't pick up a different token from
# the runner's persistent ~/.config/gh/hosts.yml first. The agent
# step below sets `GH_CONFIG_DIR` to a per-run scratch dir so gh
# starts with no stored auth and falls through to `GH_TOKEN` (the
# workflow GITHUB_TOKEN), guaranteeing comments + branch pushes
# come from `github-actions[bot]`, not whoever logged in to gh on
# the self-hosted runner host.
#
# Required-status-check note: for this workflow to actually block
# merging on drift, `Helm Sync / sync-check` must be added to the
# required status checks for `develop` (and `main`, once the
# harness is synced over there) under repo Settings → Branches /
# Rulesets. Without that, exit 1 just shows a red X but doesn't
# prevent the PR from merging.
permissions:
  contents: write          # push to helm-sync-bot/pr-<N> branch
  pull-requests: write     # gh pr comment on source PR

# One sync-check per PR at a time. A fresh push cancels the in-flight run.
concurrency:
  group: helm-sync-${{ github.ref }}
  cancel-in-progress: true

defaults:
  run:
    shell: bash

jobs:
  sync-check:
    name: Check helm parity for deploy/docker changes
    # Self-hosted runner labelled `helm-sync`, registered on the
    # vss-skill-validator coordinator host as a SECOND runner process
    # alongside the existing `vss-eval` runner. We tried ubuntu-latest
    # first but `inference-api.nvidia.com` is IP-allowlisted to NVIDIA
    # infra and refused TCP from Azure runners (run 25220887217
    # attempt 2: `API Error: Unable to connect to API
    # (ConnectionRefused)`). The vss-skill-validator host IS allowlisted
    # since it's where the coordinator already runs, and adding a
    # second runner there is $0 incremental cost. The two runner
    # processes share the box but pick up jobs by label, so a long
    # skills-eval batch on `vss-eval` doesn't block helm-sync (and
    # vice versa).
    runs-on: [self-hosted, helm-sync]

    # 60 min cap. The agent does pure file comparison + at most one
    # bot-PR push. Order-of-magnitude shorter than skills-eval (480m).
    timeout-minutes: 60

    # Only run on the mirror branches; never on develop/main.
    if: startsWith(github.ref, 'refs/heads/pull-request/')

    steps:
      - name: Checkout mirror head
        uses: actions/checkout@v4
        with:
          # full history so dorny/paths-filter and the agent can both
          # diff `PR_BASE...HEAD` against the accumulated PR diff
          # (NOT just the latest commit).
          fetch-depth: 0

      - name: Set up Python
        # Isolated Python install (pip in /opt/hostedtoolcache/Python).
        # ubuntu-24.04's system Python 3.12 has PEP 668 protection
        # which silently fails `pip install` from the agent's
        # `_ensure_sdk()` shim. setup-python sidesteps it.
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      - name: Install claude-agent-sdk
        # Pin the version we test against. Done as an explicit step
        # so a transient PyPI failure surfaces here rather than as
        # an opaque ModuleNotFoundError inside the agent script.
        run: pip install --quiet 'claude-agent-sdk>=0.0.5'

      - name: Extract PR number + base
        id: pr
        run: |
          REF="${{ github.ref_name }}"         # "pull-request/123"
          PR="${REF##pull-request/}"
          echo "number=$PR" >> "$GITHUB_OUTPUT"
          # Query the source PR for its base branch (never hardcode develop).
          BASE=$(gh pr view "$PR" --json baseRefName --jq .baseRefName)
          echo "base=$BASE" >> "$GITHUB_OUTPUT"
          echo "PR #$PR, base=$BASE"
        env:
          GH_TOKEN: ${{ github.token }}

      - name: Detect relevant changes vs PR base
        id: changes
        uses: dorny/paths-filter@fbd0ab8f3e69293af611ebaee6363fc25e6d187d # v4.0.1
        with:
          base: ${{ steps.pr.outputs.base }}
          filters: |
            relevant:
              - 'deploy/**'
              - '.github/helm-sync/**'
              - '.github/workflows/helm-sync.yml'

      - name: Run helm-sync agent
        id: agent
        if: steps.changes.outputs.relevant == 'true'
        env:
          PR_NUMBER: ${{ steps.pr.outputs.number }}
          PR_BASE: ${{ steps.pr.outputs.base }}
          PR_HEAD_SHA: ${{ github.sha }}
          PR_REPO: ${{ github.repository }}
          GITHUB_RUN_ID: ${{ github.run_id }}
          # Agent SDK auth — set as repo secrets at
          # Settings → Secrets and variables → Actions.
          # ANTHROPIC_API_KEY is required. ANTHROPIC_BASE_URL +
          # ANTHROPIC_MODEL are optional (only needed for proxies like
          # NVIDIA's inference API; omit for direct Anthropic).
          ANTHROPIC_API_KEY: ${{ secrets.ANTHROPIC_API_KEY }}
          ANTHROPIC_BASE_URL: ${{ secrets.ANTHROPIC_BASE_URL }}
          ANTHROPIC_MODEL: ${{ secrets.ANTHROPIC_MODEL }}
          # GitHub auth for the bot's git push + gh pr comment on the
          # source PR. Workflow GITHUB_TOKEN inherits the permissions
          # block above and identifies as `github-actions[bot]`.
          GH_TOKEN: ${{ github.token }}
          # Force gh CLI to use a clean per-run config dir so it
          # cannot fall through to a stored OAuth token in the
          # runner's persistent ~/.config/gh/hosts.yml. Without this,
          # gh on a self-hosted runner that someone previously did
          # `gh auth login` on will use that account's token instead
          # of GH_TOKEN — and the bot PR/comment ends up authored by
          # a real user. The fresh empty dir has no hosts.yml, so gh
          # falls through to GH_TOKEN as expected.
          GH_CONFIG_DIR: ${{ runner.temp }}/gh-helm-sync-${{ github.run_id }}
          # NVIDIA Anthropic-compatible proxy rejects `context_management`;
          # disable thinking client-side so the agent SDK doesn't emit it.
          # Harmless when using direct Anthropic.
          CLAUDE_CODE_DISABLE_THINKING: "1"
        run: |
          mkdir -p "$GH_CONFIG_DIR"
          python3 .github/helm-sync/helm_sync_agent.py
