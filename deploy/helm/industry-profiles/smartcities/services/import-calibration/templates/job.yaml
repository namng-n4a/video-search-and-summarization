# SPDX-FileCopyrightText: Copyright (c) 2025-2026, NVIDIA CORPORATION & AFFILIATES. All rights reserved.
# SPDX-License-Identifier: Apache-2.0
#
# Licensed under the Apache License, Version 2.0 (the "License");
# you may not use this file except in compliance with the License.
# You may obtain a copy of the License at
#
# http://www.apache.org/licenses/LICENSE-2.0
#
# Unless required by applicable law or agreed to in writing, software
# distributed under the License is distributed on an "AS IS" BASIS,
# WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
# See the License for the specific language governing permissions and
# limitations under the License.
#

{{- if .Values.enabled }}
apiVersion: batch/v1
kind: Job
metadata:
  name: {{ include "import-calibration.fullname" . }}
  labels:
    app.kubernetes.io/name: import-calibration
    app.kubernetes.io/instance: {{ .Release.Name }}
  annotations:
    "helm.sh/hook": post-install,post-upgrade
    "helm.sh/hook-weight": "10"
    "helm.sh/hook-delete-policy": before-hook-creation,hook-succeeded
spec:
  ttlSecondsAfterFinished: 120
  backoffLimit: 3
  template:
    metadata:
      labels:
        app.kubernetes.io/name: import-calibration
        app.kubernetes.io/instance: {{ .Release.Name }}
    spec:
      restartPolicy: OnFailure
      {{- if .Values.resourcesPvcName }}
      initContainers:
        - name: wait-for-resources
          image: {{ include "import-calibration.image" . }}
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          command:
            - /bin/sh
            - -c
            - |
              RESOURCES_ROOT="/resources"
              TIMEOUT={{ .Values.downloadResourcesWaitSeconds | default 300 }}
              ELAPSED=0
              while true; do
                READY=true
                {{- range .Values.ngcResourcesToDownload }}
                if [ ! -e "${RESOURCES_ROOT}/{{ .destPath }}" ]; then
                  READY=false
                fi
                {{- end }}
                if [ "${READY}" = "true" ]; then
                  break
                fi
                if [ ${ELAPSED} -ge ${TIMEOUT} ]; then
                  echo "[ERROR] Timeout waiting for NGC resource files (${TIMEOUT}s)"
                  exit 1
                fi
                echo "[INFO] Waiting for resource files... (${ELAPSED}s / ${TIMEOUT}s)"
                sleep 10
                ELAPSED=$((ELAPSED + 10))
              done
              echo "[INFO] Resource files present, proceeding."
          volumeMounts:
            - name: resources
              mountPath: /resources
              readOnly: true
      {{- end }}
      containers:
        - name: import-calibration
          image: {{ include "import-calibration.image" . }}
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          command:
            - /bin/sh
            - -c
            - apk update && apk add --no-cache bash curl && bash /data/calibration-import.sh
          volumeMounts:
            - name: data
              mountPath: /data
              readOnly: true
            {{- if .Values.resourcesPvcName }}
            - name: resources
              mountPath: /resources
              readOnly: true
            {{- end }}
          {{- with .Values.resources }}
          resources:
            {{- toYaml . | nindent 12 }}
          {{- end }}
      volumes:
        - name: data
          configMap:
            name: {{ include "import-calibration.fullname" . }}-data
            defaultMode: 493
        {{- if .Values.resourcesPvcName }}
        - name: resources
          persistentVolumeClaim:
            claimName: {{ .Values.resourcesPvcName }}
        {{- end }}
      {{- with .Values.nodeSelector }}
      nodeSelector:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.tolerations }}
      tolerations:
        {{- toYaml . | nindent 8 }}
      {{- end }}
{{- end }}
